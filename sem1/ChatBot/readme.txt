Before run the Py script you have to install those libraries and download this thing (+-0.5GB). Also after run Py script, code will donwload model that weight over 0.6GB 
Things to install and download:
pip install python-telegram-bot 
pip install spacy
pip install transformers torch
pip install sacremoses	-> I don't think it's necessary, but I'll leave it just in case.
python -m spacy download pl_core_news_sm
